services:
  app:
    build: .
    container_name: spoo_qr
    command: uv run uvicorn main:app --host 0.0.0.0 --port 8080 --no-access-log --reload
    volumes:
      - .:/app
      - /app/.venv
    ports:
      - "8080:8080"
    env_file: .env
